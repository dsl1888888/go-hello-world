apiVersion: v1
kind: Service
metadata:
  name: go-hello-world-service
  labels:
    app: go-hello-world
spec:
  type: ClusterIP
  selector:
    app: go-hello-world
  ports:
    - name: http
      protocol: TCP
      port: 8080        # Service port
      targetPort: 8080  # Container port
